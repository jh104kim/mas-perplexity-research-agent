# 확인필요.md

작성 시각: 2026-07-04 18:02:41 +09:00

## 현재 즉시 확인 필요

없음.

현재 Markdown 문서 기준으로 4.5 미만 품질 점수, 미완성 placeholder, 문서 간 큰 충돌은 발견되지 않았다.

## API Key 사전 점검

점검 시각: 2026-07-04 기준

| 항목 | 현재 필요 여부 | 로컬 환경변수 상태 | 비고 |
| --- | --- | --- | --- |
| `PERPLEXITY_API_KEY` | 현재 기준 불필요 | missing | Perplexity API 직접 호출로 승격할 때만 필요 |
| `PPLX_API_KEY` | 현재 기준 불필요 | missing | 공식 문서 기준 기본 변수명은 `PERPLEXITY_API_KEY` |
| `OPENAI_API_KEY` | 현재 기준 선택 | missing | OpenAI 모델을 Planner/Evaluator 등에 붙일 때만 필요 |

1차 MVP는 Mock Executor와 HITL Perplexity 결과 입력 기반이므로 API Key 없이 진행한다.

## 자동 판단한 사항

| 항목 | 판단 |
| --- | --- |
| `TODO` 표현 | `08-implementation-todo.md`의 체크리스트 제목과 항목명으로 사용된 정상 표현으로 판단 |
| `MPV` 표현 | 사용자가 말한 표현은 문서에서 일반적인 `MVP`로 정리 |
| 플러그인 사용 | 현재는 Markdown 문서 점검/수정이므로 추가 플러그인 불필요 |
| Git 상태 | 로컬 `main`과 `origin/main` 동기화 상태에서 문서만 추가 수정 중 |

## 2차 확장 전 확인 필요

아래 항목은 1차 MVP 완료 후 외부 연동이나 API 직접 호출을 검토하기 전에 확인한다.

| ID | 확인 항목 | 필요한 이유 |
| --- | --- | --- |
| CHECK-01 | HITL 결과 Markdown 파일 형식 | 사용자가 가져오는 결과가 Parser 입력으로 안정적인지 확인 |
| CHECK-02 | Perplexity API 직접 호출 승격 여부 | 현재는 보류이며, 반복 작업 부담이 클 때만 검토 |
| CHECK-03 | 산출물 저장 위치 | 프로젝트 루트 유지 또는 날짜별 폴더 도입 여부 결정 |
| CHECK-04 | Obsidian 저장 필요 여부 | MVP 이후 기능으로 승격할지 판단 |
| CHECK-05 | Slack 알림 필요 여부 | 외부 연동과 보안 범위가 늘어남 |
| CHECK-06 | 웹 대시보드 필요 여부 | CLI만으로 충분한지 재판단 필요 |
| CHECK-07 | OpenAI API 사용 여부 | 현재 설계에는 필수가 아니지만 LLM 기반 Planner/Evaluator로 확장할 때 필요 |
| CHECK-08 | Perplexity API Key 사용 가능 여부 | API 직접 호출 기능으로 승격할 때만 필요 |

## 확인 필요 발생 시 기록 방식

새 확인 사항이 생기면 아래 형식으로 추가한다.

```text
ID:
발견 시점:
관련 문서/파일:
확인이 필요한 이유:
진행에 미치는 영향:
사용자 답변:
```
